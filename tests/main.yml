---
# Tasks for testing role

- name: configure docker sandbox environment
  hosts: localhost
  roles:
    - role: amtega.docker_presets
      docker_presets_images_json_query: >-
        [? starts_with(name, `centos-6`)
           || starts_with(name, `centos-7`)
           || starts_with(name, `fedora-27`)
           || starts_with(name, `fedora-28`) ]
  tags:
    - sandbox

- name: run idempotence test
  hosts: localhost
  roles:
    - role: amtega.docker_sandbox
      docker_sandbox_state: started
  tags:
    - sandbox

- name: test mysql_jdbc_connector role
  hosts: docker_sandbox_containers
  roles:
    - amtega.mysql_jdbc_connector
  vars:
      mysql_jdbc_connector_state: present
      mysql_jdbc_connector_version: 8.0.11
      mysql_jdbc_connector_dir: /root/software
      mysql_jdbc_connector_download_dir: /root/software
      mysql_jdbc_connector_remove_download: false
  tasks:
    - name: check mysql connector download
      stat:
        path: >-
          {{ mysql_jdbc_connector_download_dir
             + "/"
             + "mysql-connector-java-"
             + mysql_jdbc_connector_version
             + ".tar.gz" }}
      register: check_download_result

    - name: check mysql connector install
      stat:
        path: "{{ mysql_jdbc_connector_jar_path }}"
      register: check_install_result

    - name: check that mysql connector was downloaded and installed
      assert:
        that:
          - check_download_result.stat.exists
          - check_install_result.stat.exists
  tags:
    - idempotence

- name: cleanup docker sandbox
  hosts: localhost
  roles:
    - role: amtega.docker_sandbox
      docker_sandbox_state: absent
  tags:
    - sandbox
